// Theme.swift

import SwiftUI

struct Theme {
    static let primaryColor = Color("PrimaryColor")
    static let secondaryColor = Color("SecondaryColor")
    static let fontName = "Montserrat-Regular" // Ensure fonts are added to your project
    static let boldFontName = "Montserrat-Bold"
    static let accentColor = Color.blue
    static let textColor = Color.primary
    static let secondaryTextColor = Color.secondary
    static let cardColor = Color(UIColor.systemBackground)
    static let backgroundColor = Color(UIColor.systemGroupedBackground)
}
